/-!
# Invalidated nullary-identity gate experiment

This file formerly compared each DSL-produced language with a second
application of `resolveNullaryPatterns`.  The `languageDef!` macro already
applies that pass, so the comparison followed from idempotence and did not
audit the unresolved authored presentation.  Those guards were tautological
and have been neutralized.

A genuine first-pass certificate requires either retaining a pre-resolution
presentation or proving a correspondence theorem about macro expansion.  This
file intentionally exports no gate or evidence claim and remains a removal
candidate pending explicit file-removal confirmation.
-/
