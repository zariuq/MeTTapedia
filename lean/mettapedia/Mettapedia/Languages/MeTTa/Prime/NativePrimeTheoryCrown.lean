import Mettapedia.GSLT.LanguageDef.NIKAdmissionDoctrineCrown
import Mettapedia.GSLT.LanguageDef.MaximalNativeCalculus
import Mettapedia.GSLT.LanguageDef.NIKCompositionCapabilitySelection
import Mettapedia.GSLT.Core.PolicyFamilyTransport
import Mettapedia.GSLT.Dynamics.ObservationPolicyFamilyUniversal
import Mettapedia.Languages.MeTTa.Prime.NIKEntryModeSelection
import Mettapedia.GSLT.LanguageDef.GSLTILUniversalStructure
import Mettapedia.GSLT.LanguageDef.GSLTILGeneratedCellReflection
import Mettapedia.Languages.MeTTa.Prime.GSLTILGeneratedCellPolicyNIKSelection
import Mettapedia.GSLT.LanguageDef.InferenceCheckedNativeWaist
import Mettapedia.Languages.MeTTa.PureKernel.Universe.PrimeKernel
import Mettapedia.Languages.MeTTa.PureKernel.Universe.MILCheckedNativePresentation
import Mettapedia.Languages.MeTTa.PureKernel.Universe.MILCheckedNativeSearch
import Mettapedia.Languages.MeTTa.PureKernel.Universe.MILCheckedNativeListPresentation
import Mettapedia.Languages.MeTTa.PureKernel.Universe.MILCheckedNativeListSearch
import Mettapedia.Languages.MeTTa.Prime.NativeRelationalSearchNIKSelection
import Mettapedia.Languages.MeTTa.Prime.NativeLiftedHypothesisNIKSelection
import Mettapedia.Languages.MeTTa.PureKernel.Universe.NativeIndexedContainerLiftingSearch
import Mettapedia.TypeTheory.NativeFamilyCapabilitySeparation
import Mettapedia.Languages.MeTTa.Prime.NativeFamilyCapabilityNIKSelection
import Mettapedia.Languages.MeTTa.Prime.NativeIndexedFamilyNIKAdmittedFlow
import Mettapedia.Languages.MeTTa.Prime.NativeIndexedFamilyConstructionNIKSelection
import Mettapedia.Languages.MeTTa.Prime.NativeIndexedFamilyGradualGuarantee
import Mettapedia.Languages.MeTTa.Prime.NativeIndexedFamilyGradualTransport
import Mettapedia.Languages.MeTTa.Prime.NativeConversionPathNIKSelection
import Mettapedia.Languages.MeTTa.Prime.NativeConversionPathGradualTransport
import Mettapedia.Languages.MeTTa.Prime.NativeGradualDisplayedCrown
import Mettapedia.Languages.MeTTa.Prime.NativeInteractionTheory
import Mettapedia.Languages.MeTTa.Prime.NativeQuotationAdequacy
import Mettapedia.Languages.MeTTa.Prime.PrimeMotivationProgramPackages
import Mettapedia.Languages.MeTTa.Prime.NativeInteractionFamilyFibration
import Mettapedia.Languages.MeTTa.Prime.NativeFibredScheduleObservation
import Mettapedia.Languages.MeTTa.Prime.NativePrimeCostInterface
import Mettapedia.Languages.MeTTa.Prime.NativePrimeCostRhoAnnex
import Mettapedia.Languages.MeTTa.Prime.GSLTILTypedOperationAdmission
import Mettapedia.Languages.MeTTa.Prime.NativeInteractionNIKAdmission
import Mettapedia.Languages.MeTTa.Prime.NativeInteractionParallelWorlds
import Mettapedia.Languages.MeTTa.Prime.NativeParallelNIKAdmission
import Mettapedia.Languages.MeTTa.Prime.NativeParallelNIKComposition
import Mettapedia.Languages.MeTTa.Prime.NativeParallelNondeterministicWorlds
import Mettapedia.Languages.MeTTa.Prime.NativeParallelGradualGuarantee
import Mettapedia.Languages.MeTTa.Prime.GSLTILMultiworldNativeRealization
import Mettapedia.Languages.MeTTa.Prime.GSLTILMultiworldPolicyNIKSelection
import Mettapedia.Languages.MeTTa.Prime.GSLTILMultiworldPolicyNIKTransport
import Mettapedia.Languages.MeTTa.Prime.GSLTILTypedEvidenceWorldNIKSelection
import Mettapedia.Languages.MeTTa.Prime.GSLTILTypedEvidenceRoutePolicySelection
import Mettapedia.Languages.MeTTa.Prime.GSLTILTypedEvidenceRoutePolicyComposition
import Mettapedia.Languages.MeTTa.Prime.GSLTILTypedEvidenceRouteProvenance
import Mettapedia.Languages.MeTTa.Prime.GSLTILGradualNativeParallelism
import Mettapedia.Languages.MeTTa.Prime.PrimeAbstractImplementationModel
import Mettapedia.Languages.MeTTa.Prime.PrimeDisplayedImplementationContract
import Mettapedia.Languages.MeTTa.Prime.PrimeImplementationPolicyTransport
import Mettapedia.Languages.MeTTa.Prime.CheckedNativeImplementationModel
import Mettapedia.Languages.MeTTa.Prime.NIKIntegratedExecutionCapabilities
import Mettapedia.Languages.MeTTa.Prime.NIKProfitabilityFrontierSelection
import Mettapedia.Languages.MeTTa.Prime.NIKPolicyFamilyCapabilitySelection
import Mettapedia.Languages.MeTTa.Prime.NIKPolicyFamilyCurrentSelection
import Mettapedia.Languages.MeTTa.Prime.NIKPolicyFamilyTransport
import Mettapedia.Languages.MeTTa.Prime.NativeCheckedHypothesisPolicyNIKSelection
import Mettapedia.Languages.MeTTa.Prime.GSLTILTypedOperationComposition
import Mettapedia.Languages.MeTTa.Prime.GSLTILInteractionTransport
import Mettapedia.Languages.MeTTa.Prime.GSLTILInteractionTransportComposition
import Mettapedia.Languages.MeTTa.Prime.GSLTILLinearEffectTransport
import Mettapedia.Languages.MeTTa.Prime.GSLTILLinearEffectTransportComposition
import Mettapedia.Languages.MeTTa.Prime.GSLTILNativeParallelRealization
import Mettapedia.Languages.MeTTa.Prime.NativeTypedOptimizationAdmission
import Mettapedia.Languages.MeTTa.Prime.NativeTypedOptimizationNIKBridge
import Mettapedia.Languages.MeTTa.Prime.NativeTypedOptimizationImplementationModel
import Mettapedia.Languages.MeTTa.Prime.NativeTypedOptimizationInstances
import Mettapedia.Languages.MeTTa.Prime.NativeTypedOptimizationCapabilityNIKSelection
import Mettapedia.Languages.MeTTa.Prime.IncrementalCompressionOptimizationSelection
import Mettapedia.Languages.MeTTa.Prime.IncrementalCompressionExternalGuidanceReceipt
import Mettapedia.Languages.MeTTa.Prime.IncrementalCompressionExternalArtifactChecker
import Mettapedia.Languages.MeTTa.Prime.IncrementalCompressionRankingArtifact
import Mettapedia.Languages.MeTTa.Prime.IncrementalCompressionBoundedRanking
import Mettapedia.Languages.MeTTa.Prime.IncrementalCompressionEvidenceStageBridge
import Mettapedia.Languages.MeTTa.Prime.NativeTypedOptimizationSelectedImplementationModel
import Mettapedia.Languages.Metamath.InferenceAssertionFusedCompilation
import Mettapedia.Languages.Metamath.InferenceAssertionSliceCompilation
import Mettapedia.Languages.Metamath.InferencePreparedAssertionStepReceipt
import Mettapedia.Languages.Metamath.InferenceGeneratedProvesReceipt
import Mettapedia.Languages.MeTTa.Prime.NativeTypedMetamathAssertionOptimization
import Mettapedia.Languages.MeTTa.Prime.NativeTypedMetamathPolicyNIKSelection
import Mettapedia.Languages.MeTTa.Prime.NativeTypedMetamathImplementationPolicyTransport
import Mettapedia.Languages.MeTTa.Prime.NativeProgramGradualGuarantee
import Mettapedia.Languages.MeTTa.Prime.GradualDependentCapability
import Mettapedia.Languages.MeTTa.Prime.NativeGradualDependentTermFormers
import Mettapedia.Languages.MeTTa.Prime.NativeGradualDependentNaturality
import Mettapedia.Languages.MeTTa.Prime.NativeDependentReceiptComputad
import Mettapedia.Languages.MeTTa.Prime.NativeDependentReceiptCoherenceObservation
import Mettapedia.Languages.MeTTa.Prime.NativeDependentReceiptCoherenceNIKAdmission
import Mettapedia.Languages.MeTTa.Prime.NativeDependentReceiptSubstitutionNIKSelection
import Mettapedia.Languages.MeTTa.Prime.NativeGradualDependentInteraction
import Mettapedia.Languages.MeTTa.Prime.NativeGradualDependentGuarantee
import Mettapedia.Languages.MeTTa.Prime.NativeGradualCapabilityNIKSelection
import Mettapedia.Languages.MeTTa.Prime.NativeProgramSourceAdequacy

/-!
# Prime source-to-licensed-execution theory crown

This is the intentional import gate for the current Prime theory tranche.  A
successful build checks one connected dependency graph containing:

* the revision-fibred NIK admission doctrine: proof-relevant semantic and
  observed refinement categories, dependency reindexing, common-current
  composition, direct active execution, and its non-faithful endpoint shadow;
* request-fibred maximal-native selection: exact capability requests cannot
  hide eligible realizations, request-local common-upgrade closure yields one
  unique strongest admitted operation, and an incomparable negative retains a
  genuine maximal frontier rather than an arbitrary dispatcher priority;
* an actual NIK proof-kernel instance: native checking and explicit boundary
  replay preserve the same proof judgment but distinct route receipts, each is
  strongest only in its exact request fibre, and a neutral request has no
  canonical strongest face; dependency-current activation runs the retained
  operation directly, while a relevant change prevents activation;
* constructional indexed-family admission: a complete typed authored
  occurrence is equivalent to its complete proof-relevant native receipt,
  the forward equipment cell runs as revision-current certificate-free
  admitted flow, and substitution commutes with that active construction;
  a typed canonical occurrence for every authored schema generates a
  judgment-indexed constructional kernel whose typed substitutions retain the
  exact authored position, both endpoint derivations, and native receipt, and
  whose complete conversion trees realize structurally into the existing
  native computation; List/identity and the two candidate views of the shared
  Nat/Vec declaration world satisfy this interface, while the raw syntactic
  boundary remains outside constructional authority; NIK compares that
  constructional face with uniform raw preservation only after the latter's
  declaration theorem is supplied, selects it uniquely for raw-preservation
  requests, and otherwise retains raw input for fallback rather than replay;
  construction evidence is displayed over that unchanged raw request, so
  exact current evidence selects the same no-replay native operation while
  suspension, local refutation, or revision staleness returns the complete
  request to fallback; changing the dependency revision cannot activate the
  cached operation, and no second evaluator or checker is introduced;
  typed contextual substitution composes the retained schema substitution and
  endpoint derivations, its constructional erasure square commutes on the full
  raw request, request substitution satisfies identity and composition, and
  the resulting exact map preserves exact evidence while safely suspending
  unsupported blame; a nonidentity List weakening realizes only when the
  construction witness is present and otherwise retains fallback;
  equation closure remains a distinct capability requiring independent
  preservation rather than being inferred;
  proof-relevant conversion trees are the unique structural extension of
  their step, reflexivity, symmetry, and transitivity interpretations, so an
  exact equivalence of primitive step fibres lifts generically to complete
  path equivalence; the indexed-family instance preserves every intermediate
  typed state, authored occurrence, native receipt, and association tree under
  composition and typed substitution; current NIK admission realizes those
  paths directly; exact native-path evidence is displayed over the complete
  authored conversion tree, typed substitution transports that evidence by the
  receipt-naturality square, and the gradual exact branch agrees with NIK's
  selected complete-path face; suspension, unsupported blame, and revision
  staleness retain the authored tree for fallback, while a nonidentity List
  weakening preserves the whole three-leg native tree; a request-local native
  family proves that complete structural lifting strictly extends
  primitive-step-only realization, selects the lift as the unique strongest
  complete-path face, and retains the entire authored path on weaker-face
  fallback, while native construction and explicit boundary replay remain
  incomparable without a boundary request; same-endpoint association trees
  remain distinguishable; operational GSLT comparison cells
  and dependent structural-conversion cells are both exactly equivalent to one
  common free-whiskered-cell syntax, whose interpretations form a contractible
  type once the five raw constructors are interpreted; this relative
  initiality preserves complete one-cell and two-cell trees while leaving unit,
  associativity, interchange, substitution, observation, and all higher
  coherences to an explicit later completion;
  List, Nat, and Vec inhabit the common bridge, while stale admission and a raw
  ill-typed equation supply independent refusing controls;
* the observation-relative coherence boundary for dependent receipt cells:
  authored-generator count is invariant under vertical units and
  reassociation; the two raw horizontal interchange readings also have equal
  authored-generator and total-constructor counts, while complete
  raw-constructor count detects an inserted unit and complete raw shape detects
  both reassociation and interchange even when both scalar counts agree; a
  readout supports a nonempty policy family exactly when every
  pair it identifies agrees under every requested policy, while the
  constructive admission record retains the actual runners rather than using
  that extensional theorem as an oracle; the concrete dependent receipt admits
  its compressed key for a unit-insensitive request, refuses every unit- or
  reassociation-identifying key and every interchange-identifying horizontal
  key for the corresponding history-sensitive request, and retains the raw
  cells as sufficient keys for all observations, so a later strict, weak, or
  higher completion is licensed per declared observation rather than imposed
  as a global quotient; the corresponding NIK boundary constructively admits
  generator count for the small request and exact cell retention for the full
  request, proves the lossy face unadmittable for the full request, runs the
  current retained function directly, and preserves the raw cell on staleness;
* strict substitution transport for proof-relevant dependent receipt cells:
  a generator-naturality witness extends structurally over the complete free
  cell and preserves authored-generator count, constructor count, and raw
  shape; an empty target-generator fibre cannot acquire that capability; NIK
  selects the target-cell constructor as the unique strongest exact face,
  activates it only at its retained revision, and leaves complete source-cell
  fallback available after staleness; composability of successive transports
  is a stronger root-computation capability; the canonical retained Prime root
  now proves identity and composition coherence, and those laws lift through
  every structural step constructor and every complete conversion tree without
  erasing intermediate terms or receipt shape; a legal computation that
  preserves identity substitution but violates composition proves that this
  stronger license cannot be inferred from one-step substitution actions;
  functoriality of the authored comparison-generator action is proved
  independently, and the two laws together identify successive and direct
  substitution on every complete dependent cell, including every generator,
  vertical node, and whiskering; NIK exposes exact retention, sequential
  transport, and direct composite transport as three request-local faces, makes
  the direct face uniquely strongest only when both laws are supplied, disables
  it after revision change, and refuses it for a generator action whose local
  maps fail composition; this is semantic capability, with profitability still
  an independent admission question;
* the layered GSLT-IL universal structure: authored generator interpretations
  have a contractible space of structural path extensions, while relational
  elaboration and occurrence evidence remain first-class when erasure is
  non-faithful; unique structural extension is separated from reflection:
  a fibrewise generator retraction makes the complete generated-cell map
  injective, a fibrewise equivalence lifts to an exact cell equivalence, and a
  single primitive collision gives a contractible-but-non-reflecting negative;
  split reflection supports every dependent policy family, while a structural
  quotient may still serve shape-only policies and is excluded exactly when a
  requested policy distinguishes collided receipts; the resulting exact NIK
  request selects the retained admitted operation without inserting replay;
  capability-indexed multiworld observers may retain complete
  outcome/history worlds, visible outcomes, or cardinality, and a policy that
  distinguishes histories cannot factor through the visible-outcome quotient;
  these faces now form an exact request-local NIK family over one unchanged
  retained world history, so a complete-history request uniquely selects the
  full-world face, current execution preserves every distinct history and its
  order, staleness preserves the same history for fallback, and the existence
  of that strongest observation face does not imply a functional elaborator
  for the ambient raw relation; complete elaboration-world transport is an
  additional typed-route capability carrying maps of internal outcomes and
  their evidence fibres, and its identity and composition laws lift to exact
  ordered-history transport; target policy families pull back along that map,
  so a complete-target-world request again selects the full-world face and
  current NIK activation runs the retained transport directly, whereas stale
  activation preserves the unmapped source history; an independent
  world-injectivity proof lifts to history reflection, while a checked
  non-injective evidence map preserves two occurrence positions but identifies
  their histories, proving that successful full-world transport alone cannot
  mint source reflection; a typed proof-world route now displays this evidence
  action over one structural presentation morphism, carries an executable
  internal map proved equal to the structural symbol action, and proves surface
  naturality; primitive routes extend by identity and composition over the
  intrinsic finite route-program syntax, with structural projection exactly
  the existing free-path interpretation; restricting the resulting route at
  an authored command derives NIK's complete-world transport and current
  strongest execution rather than supplying an unrelated function; primitive
  reflection capabilities compose to program-level injectivity, while two
  interpretations of the same typed `promote` program and structural morphism
  respectively retain and collapse proof histories, so neither route syntax
  nor structural preservation mints reflection; reflection is then weakened
  exactly to the consumer's declared policy family: support is equivalent to
  factorization of the complete answer vector through the mapped target
  history, retained runners install directly into NIK's ordinary policy
  catalog, route composition cannot manufacture support lost by its prefix,
  and the same non-injective route admits history length while refusing exact
  source-history replay; executable route-policy realizations compose through
  the residual policy family left by their prefix, and composite support is
  exactly suffix support on the propositionally certified reached intermediate
  image;
  extending that equivalence to every ambient intermediate history requires
  prefix surjectivity, with a non-surjective checked counterexample where the
  composite remains adequate but the unrestricted suffix residual is not;
  this policy image is explicitly separated from the provenance-retaining
  reached-history carrier, which stores both endpoint histories as data,
  forgets injectively exactly when the route reflects complete histories,
  refuses exact provenance replay for a checked collapsing route, and
  transports coherently through reassociated route composition;
* compile-time MeTTa expression and complete-program quotation;
* proof-carrying raw, eager, and suspended program-island preparation;
* the dependent rho interaction fibre and its non-collapse boundary;
* finite-family separation, occurrence-indexed conflict colourings, and
  work/span schedules;
* capability-indexed execution observation factored through retained event
  containers, semantic values, and explicitly lossy scheduler readouts;
  exact chronological cost layer wave-event histories form the witness container,
  WorkSpan is only their declared valuation, two valid histories may have equal
  WorkSpan and unequal receipts, and any genuine operational realization pulls
  this factorization back to selected cost layer normalization paths;
* request-scoped NIK admission for those real cost layer observations: WorkSpan is
  an executable key for a WorkSpan-only request but is refused for a request
  that also observes receipt chronology; the complete policy-answer vector is
  the least informative sufficient key, current activation evaluates it without
  interior checking, and staleness disables that view while preserving the
  complete history for fallback;
* policy-family sufficiency as a universal property: the complete vector of
  declared policy answers is the least informative sufficient readout, so a
  cheaper scheduler view is licensed exactly when that vector factors through
  it;
* exact product requests for native computational structure and dependent
  policy families: maximal, strongest, and profitability-frontier selection
  reuse one capability fibre and retain executable policy runners, while an
  unsupported or incomparable request remains explicitly so;
* common-revision activation of that selected product: one dependency witness
  activates the original native operation and every requested policy runner,
  while staleness disables both and preserves the complete fallback input;
* that common selected-current product instantiated by checked intrinsic
  hypotheses: both the learned grandparent chain and the strictly-positive
  `List.mapRel` plan retain their exact proof-result and occurrence-count
  policies, while a result-only key provably cannot answer an execution-face
  policy; wrong-middle and missing-tail programs never reach selection, and
  revision staleness preserves the complete query and result for fallback;
* intrinsic `Compρ` inhabitants as the observed execution family, preserving
  endpoint indices, occurrence histories, and non-gating partial observation;
* revision-indexed NIK admission of intrinsic `Compρ` execution through a
  common equipment cell: event count descends to ordinary semantic paths,
  while exact provenance and site-sensitive policies provably do not;
* the native multiworld boundary: separation licenses one concurrent wave,
  while contested communication retains distinct raw rho branches without
  manufacturing parallel authority or a sequential schedule;
* revision-indexed admission of finite separated families and arbitrary valid
  conflict colourings into operational schedules, preserving exact receipts
  and WorkSpan by independently proved observation squares;
* common-current composition from coloured families through executable
  schedules to observation-retaining logical traces, with semantic admission
  independent of profitability;
* worldwise native realization over rho nondeterminism: certified alternatives
  compile independently, raw alternatives survive unchanged, ordered world
  occurrences are retained in capability-indexed witness containers, semantic
  values preserve targets plus optional native WorkSpan, target-only scheduler
  readouts are provably lossy, and contested alternatives cannot mint a
  cross-world parallel certificate;
* proof-relevant gradual precision inside each rho world: certification
  refines its exact raw branch, deoptimization recovers that branch, ordered
  target occurrences are invariant, and contested communications with
  different targets cannot be confused with precision refinement;
* orthogonal elaboration and execution worlds: an authored meaning relates
  loosely to zero, one, or many rho execution worlds, native realization
  postcomposes only on the execution axis, and the composite retains the
  elaboration world, raw execution world, and both witnesses; contested rho
  branches remain distinct beneath one shared authored meaning;
* deoptimization-closed relational interpretation: every semantics over raw
  rho branches lifts canonically across native annotations, while authored
  ambiguity and runtime nondeterminism remain independent multiplicity axes;
* relational elaboration worlds, retained proof-history multiplicity, exact
  typed selection, represented route execution, and revision-current NIK
  admission;
* one common displayed checked-native waist whose exact-erasure carrier adds
  no proofs and rejects no checked proofs, whose retained graph preserves
  semantic evidence even when its hot artifact projection is lossy, and whose
  exact faithfulness criterion is proved rather than assumed;
* checked learned-presentation extensions with independent proof-relevant
  semantics, exact raw-proof retention, intrinsic indexed hypothesis
  elaboration over formed contexts, and revision-current NIK admission; every
  checked relational derivation in the learned presentation crosses that
  exact image into a typed native program, while the rejected wrong-middle
  proof cannot cross it;
* exact finite native search derived structurally from intrinsic hypothesis
  programs: primitive evidence-fibre providers compose through dependent
  relational chaining, retaining each intermediate value and both premise
  derivations; a checked raw proof constructs this capability once at ingress,
  after which native execution enumerates the complete occurrence bag without
  replaying the checker; functional representation is proved sufficient but
  not necessary, while an infinite proof fibre supplies the refusing boundary;
* checked recursive List-relator extensions as a distinct exact-image
  consumer: the independently interpreted head/tail proof spine becomes a
  term of Prime's native strictly-positive `List.mapRel` family, with exact
  raw-proof retention and a missing-tail rejection control;
* exact finite search closed under the native List relator: a finite element
  evidence fibre induces a finite List fibre whose cons indices retain the
  head occurrence and recursive tail occurrence; functional evidence earns
  the graph of native `List.map`, finite nondeterministic evidence preserves
  every proof occurrence, and an infinite singleton head fibre proves that
  strict positivity alone cannot manufacture a finite-search capability;
* generic higher-order relation lifting with capabilities kept honest and
  orthogonal: finite-search preservation and functional-representation
  preservation transport across declared carrier equivalences and recurse
  through primitive, chain, and lift hypotheses; native polynomial List earns
  both, while an infinitary strictly-positive product has a compositional
  relator but provably cannot advertise finite search; the resulting generic
  hypotheses select finite or direct NIK realizations exactly when current;
* indexed-container generation of those capabilities: every indexed-container
  description generates an exact compositional proof-relevant relator and
  functional mapping realization, while a separate finitarity witness
  generates complete finite occurrence search; List supplies the positive
  two-branch canary, and the positive infinitary reader retains lifting and
  mapping while proving that finite search is unavailable;
* theorem-level separation between recursive and parameter capabilities:
  polynomial List has both its recursive indexed-polynomial structure and an
  exact unary parameter-container representation, while a second indexed
  polynomial stores its parameter negatively and therefore has neither a
  covariant parameter map nor any unary container representation; recursive
  positivity, parameter covariance, and finitary search are consequently
  independent licenses rather than one datatype flag;
* exact transport from those family representations into request-local NIK
  selection: polynomial List retains both nondeterministic answer occurrences
  and selects exhaustive finite search; the positive infinitary reader selects
  direct realization for a functional relation while refusing general finite
  enumeration; exact graph agreement identifies direct realization with the
  transported family map, and a bidirectional theorem characterizes general
  finite-search preservation exactly by finitary parameter positions; the
  parameter-negative family cannot construct the covariant bridge, so absence
  of one capability never becomes semantic rejection or a fabricated weaker
  license;
* the checked List-relator boundary composed with that structural provider:
  accepted raw proof identity, authored endpoints, intrinsic native typing,
  complete answer enumeration, and occurrence membership remain jointly
  inspectable, while hot execution consumes only the constructed provider and
  a missing recursive premise cannot construct a native-search plan;
* request-local maximal-native selection connected to revision-indexed NIK
  admission over one common complete-occurrence contract: represented relations
  select direct map, finite nondeterministic relations select exact exhaustive
  search without claiming functionality, activation runs the chosen operation
  unchanged, dependency change prevents reuse, checked evidence survives the
  selected run, and an infinite proof fibre remains outside finite selection;
* proof-relevant transport of authenticated interaction histories along typed
  GSLT-IL routes: exact path erasure and established commuting squares are
  preserved, while raw nondeterminism and target-native separation remain
  deliberately distinct capabilities;
* identity and composition laws for interaction transport at the full
  occurrence-history level, including non-injective routes whose visible
  target endpoints do not reflect source distinctions;
* linear-effect transport along GSLT-IL routes: a common multiset homomorphism
  preserves pair separation even when resource names collapse, while an
  explicit contraction counterexample isolates occurrence loss as the true
  boundary; pointwise consumption agreement transfers the certificate to an
  independently authored target analysis, while the target's operational
  diamond remains a separate native capability;
* compositional linear-effect transport: identity and composite resource maps
  agree on exact effects and separation data, successive occurrence wrappers
  are equivalent to one composite wrapper, and a non-injective later route is
  explicitly shown not to reflect erased intermediate resource distinctions;
* target-native realization of transported parallelism: contextual occurrence
  execution plus a separation realizer constructs both target execution orders
  and their common endpoint before WorkSpan is read, while an execution-less
  negative proves that separation and analysis agreement cannot mint a
  commuting diamond;
* an abstract implementation-model contract over proof-relevant execution
  traces: exact raw and receipt codecs, revision-current observed refinement,
  compositional hot execution, independent fallback, and observation
  agreement; authored receipt fibres retain GSLT-IL elaboration evidence that
  runtime traces cannot reconstruct, while a separate negative proves that
  target-only receipts cannot recover native schedule provenance;
* the displayed gradual instance of that implementation contract: complete
  program plans form an exact source codec and island promotion decodes to the
  unchanged program; one gradual rho state becomes one worldwise source trace,
  current exact evidence compiles to its certified schedule, and suspension,
  blame, or evidence staleness compiles as the exact raw branch; implementation
  staleness independently prevents activation while its exact fallback remains
  decodable; the contested two-world trace retains both ordered occurrences
  and distinct targets, so strongest-realization selection cannot become
  semantic branch selection;
* contravariant transport of admitted policy families along that implementation
  model's complete trace map: current activation runs the identical target
  policy after compilation, composition is exact, and maximal or strongest
  selections remain so in the transported request fibre; staleness disables
  the pulled policy while preserving raw fallback, whereas a non-surjective
  implementation gives a checked counterexample to policy reflection and
  requires fresh recognition for any stronger image-specific native face;
* the generic checker/native/implementation triangle: an exact-erasing checked
  ingress is a universe-polymorphic source state, its retained semantic-evidence
  and native-artifact graph is the target state, and revision-current execution
  constructs that graph without a checker argument; fallback recovers the full
  authored proof and checked derivation, receipts recover the full graph, and
  artifact-only receipts are exact only when the projection is faithful; the
  checked grandparent proof crosses the whole triangle while the wrong-middle
  proof cannot enter it and staleness preserves the checked fallback;
* declaration-hosted checked ingress as a strict maximal-native boundary:
  every accepted formed-typing program realizes conservatively in any authored
  host, including the native List/identity world, while a genuinely declared
  constant is constructible in that hosted DTT and provably absent from the
  fixed bare checker image; source-generated boundary rules may therefore add
  raw ingress without redefining native construction as checker replay;
* that source-generated ingress realized concretely: first-wins authored
  constants form an executable proof-carrying inventory whose collision-free
  ground facts are structurally admitted as a new-judgment extension; checked
  acceptance reflects to the exact active source occurrence and cannot mint a
  proof for a later shadowed declaration, while native hosted construction
  remains the stronger no-replay route after ingress;
* the parallel equation boundary completed without weakening that distinction:
  accepted ground facts authenticate exact full-source and equation-inventory
  positions, retain duplicate occurrences, and reject positions occupied by
  constants; a checked counterexample proves that ground Pattern-schema
  instantiation cannot perform Prime object substitution; authenticated schemas
  instead enter the native typed-conversion fibre through simultaneous
  substitution plus endpoint typing, uniform preservation constructs the target
  when available, and the native receipt reflects the exact source occurrence
  while an ill-typed raw instance remains outside the fibre;
* the cost-layer iteration cache and replay boundary: a key supports a policy exactly when
  that policy factors through it, exact replay supports every future policy,
  and collisions preclude exact replay; rho's concrete cost-layer iteration fibre makes its
  compact term unsafe for a future observation while the proof-relevant
  provenance key supports the same observation exactly;
* the corresponding global implementation-key theorem over the complete
  dependent Cost carrier: compact-key policy safety is exactly fibre
  invariance, compact syntax is an exact receipt exactly when its erasure is
  faithful, and faithfulness constructs an exact codec; rho's cost-layer iteration witness
  still permits the complete compact value as a policy-only hot key while
  refusing that same key for exact replay, which the displayed carrier admits;
* the unconditional selected rho cost-layer iteration seal: the normalizer configuration is
  definitionally the compact output of the exported rho cost layer domain object,
  its representative premise is proved, and its second-layer displayed carrier
  has a genuinely nontrivial elaboration fibre; hence the complete
  nonfactorization, compact-policy, provenance-replay, and NIK refusal package
  applies without a caller-supplied configuration or a selected compact cost-layer iteration
  executor;
* the induced information preorder on observation keys: exact keys refine
  every coarser representation, supported policies are monotone in retained
  information, and rho's provenance key strictly refines its compact key;
  two elaborations in the cost-layer iteration witness collide compactly while a policy
  distinguishes them, forcing every compact decoder to misreplay at least one;
* revision-current NIK admission of request-scoped receipt keys, displayed
  over rather than replacing an admitted semantic execution model: retained
  keyed functions run without interior checking, stronger keys inherit safe
  runners, staleness preserves raw fallback, the policy vector is the least
  informative policy-only key, and exact replay forces equivalence with the
  full state; rho's compact cost-layer iteration key is refused for both its distinguishing
  policy and exact replay while the provenance key admits both requests;
* one common-current display of semantic execution admission, request-scoped
  receipt sufficiency, and optional profitability: the hot runner uses only
  the retained semantic map and keyed policy, neither receipt nor cost evidence
  can manufacture the other, and staleness disables the display without
  revoking raw fallback; exact maximal-native capability fibres cannot quietly
  delete two semantically indistinguishable licensed candidates according to
  an external cost preference, so profitability ranks already-admitted
  realizations instead of masquerading as calculus strength;
* profitability selection over the exposed semantic maximal frontier: every
  feasible finite request has a nonempty frontier and every declared linearly
  ordered cost has a minimum there; the selected operation remains the
  original admitted arrow, a cheaper non-maximal realization cannot be
  promoted, and equal-cost symmetry-related realizations have no hidden
  invariant winner, requiring an explicit tie policy or retained alternatives;
* generic typed optimization admission in which each hosted calculus supplies
  a proof-relevant native judgment indexed by the exact source and complete
  revision key, while shape recognition and profitability remain independent;
* derivation of every extensional native typed optimization specification as
  an ordinary NIK optimization family: exact source occurrences compile to
  artifact terms, independent adequacy supplies the observed refinement
  square, native and generic preparation agree, full-key equality governs
  currentness, cross-family alignment is impossible, and WorkSpan receipts
  descend only after semantic admission;
* conservative request-local maximal-native selection for those typed
  optimizations: observational adequacy alone leaves ordinary and compiled
  faces incomparable, an explicit compiled-artifact request selects the
  compiled face uniquely, and a declared cost projection ranks only the
  neutral maximal frontier without manufacturing calculus strength; aligned
  source/key authority plus recognized shape is necessary for eligibility, so
  a retained-reference storage judgment and a stale Metamath revision remain
  explicit refusing controls;
* coherence of that strongest request with the abstract Prime implementation
  model: both hot interfaces emit the identical compiled artifact, independent
  adequacy closes their common observation triangle, and the complete
  optimization key is one currentness boundary for both; changing it disables
  both active views while the exact source trace remains available for raw
  deoptimization, and missing shape recognition prevents construction of the
  selected implementation path;
* realization of every such admitted typed optimization as an abstract Prime
  implementation model: discrete proof-relevant traces have exact source and
  artifact codecs, hot execution uses only the retained current refinement,
  and any complete-key change prevents activation while preserving exact raw
  fallback;
* three structurally independent optimization instances through that common
  family: deterministic dispatch carries a key-bound cardinality/type
  judgment, storage carries its actual lifetime/publication classification,
  and memoization carries its physical snapshots and append-only derivation;
  source-derived Metamath operation-record selection is then licensed by
  unique production labels, call-local storage reuse by nonescape and value
  publication, and memo reuse by exact snapshot stability; duplicate labels,
  retained references, changed snapshots, and cross-family authority are
  explicit refusing controls, while profitability remains a separate receipt
  rather than semantic authority;
* the exact Metamath assertion-fusion adapter: projected unique floating
  variables turn relational finite substitution into deterministic lookup,
  generic allocation-free template consumption is equivalent to every
  essential-hypothesis and conclusion substitution, and disjoint-variable
  evidence is retained unchanged; a duplicate-key counterexample proves the
  functionality premise necessary rather than silently choosing one source
  derivation;
* exact Metamath slice and region lowering: ordered replacement formulas and
  assertion templates pack into one immutable carrier, compact body subslices
  agree with relational lookup and fused matching, persistent slices retain
  their owner, and resettable call-local regions preserve every ordered query
  observation while refusing reference publication and duplicate-key
  authority;
* prepared stored-assertion selection as a separate certified layer from
  grammar operation records: successful projection supplies unique assertion
  labels, immutable indexed lookup returns the exact complete `AssertionView`,
  and one proof-relevant receipt retains the generated inference node, exact
  authored-to-runtime substitution correspondence, slice-backed fused match,
  and complete live verifier step; end-to-end theorems join that occurrence to
  revision-keyed NIK admission and the abstract implementation-model contract;
  the receipt construction lifts over complete generated proof trees, retaining
  every intermediate stack invariant, active-hypothesis occurrence, assertion
  substitution, and exact final execution state; duplicate authored labels
  remain duplicate receipt occurrences rather than collapsing to a set, every
  assertion occurrence receives its own current admission, and a revision
  change makes all of those admissions stale while the complete ordinary proof
  execution remains available with the identical result; malformed
  duplicate-label snapshots likewise retain ordinary first-occurrence scanning
  but are refused index authority;
* the same Metamath lookup realized as a concrete maximal-native policy
  family: the ordinary ordered scan and immutable compiled index inhabit one
  exact observation fibre, the executable compiled-lookup/source-lookup
  equality grounds the index's strictly stronger capability, and exact-result
  plus occurrence-count policies activate with it at one revision; a
  result-only key cannot recover execution face, staleness preserves the full
  query and receipt, an adversarial cost cannot promote the cheaper
  non-maximal scan, and a duplicate-label projection cannot enter selection;
  fused substitution remains a subsequent admitted transformation rather than
  an incomparable operation hidden in the same ranking;
* the corresponding concrete implementation-policy square: an exact authored
  occurrence compiles to an artifact trace whose complete lookup receipt is
  equal to the strongest prepared lookup receipt, retaining physical queries,
  ordered results, multiplicity, and execution face; exact and count policies
  run through the same full optimization key with no interior checker,
  staleness disables them while preserving raw fallback, and duplicate source
  labels cannot construct a transported selection; the fixed compiled image
  can freshly recognize its prepared face, but policy transport itself is
  proved not to mint that stronger image-specific capability;
* the dependent gradual capability law: raw semantics remains the unchanged
  index of suspended, exact, and refuted evidence; exact evidence transports
  through formed-context substitution without checking, while stable blame
  transports only under an explicit exact-reflection capability; first demand
  uses the shared proof-relevant Need protocol, and revision change invalidates
  the cached capability without removing the raw term;
* constructional gradual lifting of the actual cumulative Prime term formers:
  products of displayed capabilities feed independent Pi-application premises,
  dependent sums retain the first-indexed second premise of Sigma pairing,
  and lambda, application, pairing, identity formation, and reflexivity build
  their result judgments directly; precision is monotone, a missing declared
  body cannot manufacture a typed lambda, and the raw lambda remains available;
* substitution naturality of those constructional capabilities: Pi and
  first-Sigma beta retain strictly commuting proof-relevant receipts, while
  dependent second-Sigma beta retains its distinct type-conversion and term
  computation dimensions; its raw boundary and term receipt commute, and the
  comparison of substitution-expanded type-receipt trees is honestly left to
  an explicit higher coherence cell rather than erased by proof irrelevance;
  native identity iota cells enter the same gradual exact fibre directly, and
  changing the authored iota target is an uninhabited negative control;
* the proof-relevant interaction instance of that law: exact occurrence paths
  compose monotonically and transport along typed GSLT-IL routes, intrinsic
  `Compρ` composition constructs exact evidence with chronological provenance,
  indexed protocol mismatch is uninhabited, and many-to-one transport retains
  occurrences while unsupported blame safely returns to suspension;
* the full lazy gradual-dependent law package: safe constructional transport
  composes, reflecting transport preserves proof-relevant blame functorially,
  revision activation commutes with independent and dependent construction,
  every exact square acts naturally on gradual states, ordinary checked
  execution has no checker parameter, and the forward-only collapse proves
  that stable-blame transport is a strictly stronger earned capability;
  the two faces inhabit one request-local maximal-native contract: every exact
  map has a unique strongest safe selection, an explicit reflection
  registration strictly upgrades it and is uniquely strongest for exact-state
  and stable-blame requests, a non-reflecting collapse cannot construct that
  registration or a blame-sensitive request, and revision-current NIK
  activation executes the retained face directly;
* the shared displayed operational crown: complete authored programs are raw
  indices with source-preserving plans as exact evidence, island promotion is
  an identity-on-source exact map, and valid evidence for a different
  candidate cannot cross-promote an occurrence; proof-relevant rho branches
  are likewise raw indices with certified schedules as exact evidence, so
  suspension, local blame, and revision staleness recover the exact raw branch
  while current evidence realizes its native schedule; competing
  communications remain distinct raw indices and therefore cannot be selected
  or merged by gradual precision;
* the program-level gradual guarantee derived from the native seam; and
* source-occurrence adequacy from authored programs through Prime interaction
  terms to ordinary authorized GSLT paths.

The gate does not claim a CeTTa implementation of these interfaces.  It
collects the reference semantics and theorems that such an implementation
must refine.
-/
